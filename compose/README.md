# Web App for the `compose_spatial.py` Program

This directory contains files required to run the interactive graphical user interface of the `compose_spatial.py` program. Please see the documentation of `compose_spatial.py` and its `--serve` option for usage.
